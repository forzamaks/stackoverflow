$('.form-errors[data-form-id="<%= @answer.id %>"]').html('<%= render 'shared/errors', resource: @answer %>');
<% if !@answer.errors.present? %>
   $('.answer[data-answer-id="<%= answer.id %>"]').replaceWith('<%= j render answer %>');
<% end %>

