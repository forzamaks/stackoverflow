- if current_user&.subscribed?(resource)
  = link_to 'Unsubscribe', subscription_path(resource), method: :delete, remote: true, class: 'btn btn-danger'
- else
  = link_to 'Subscribe', question_subscriptions_path(resource), method: :post, remote: true, class: 'btn btn-success'